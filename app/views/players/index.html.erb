<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="grid grid-cols-1 gap-8">   
    
    <div class="bg-white rounded-lg shadow-sm">
      <!-- ヘッダー -->
      <div class="px-6 py-4 border-b border-gray-200 flex justify-between items-center">
        <h2 class="text-xl font-semibold text-gray-900">選手一覧</h2>
        <%= link_to "選手作成", new_player_path, class: "px-4 py-2 bg-black text-white rounded-md hover:bg-gray-800 text-sm font-medium" %>
      </div>

      <!-- 検索フォーム -->
      <div class="px-6 py-4 border-b border-gray-200 bg-gray-50">
        <%= search_form_for @q, url: players_path, method: :get, class: "flex flex-wrap gap-4 items-end" do |f| %>
          <div>
            <%= f.label :name_cont, "名前", class: "block text-sm text-gray-700" %>
            <%= f.search_field :name_cont, class: "border rounded p-2 focus:outline-none focus:ring focus:border-blue-300", data: { field: "name" } %>
            <ul class="autocomplete-results hidden absolute bg-white border w-40 rounded mt-1 z-10"></ul>
          </div>

          <div>
            <%= f.label :team_cont, "チーム", class: "block text-sm text-gray-700" %>
            <%= f.search_field :team_cont, class: "border rounded p-2 focus:outline-none focus:ring focus:border-blue-300", data: { field: "team" } %>
            <ul class="autocomplete-results hidden absolute bg-white border w-40 rounded mt-1 z-10"></ul>
          </div>

          <div>
            <%= f.label :position_cont, "ポジション", class: "block text-sm text-gray-700" %>
            <%= f.search_field :position_cont, class: "border rounded p-2 focus:outline-none focus:ring focus:border-blue-300", data: { field: "position" } %>
            <ul class="autocomplete-results hidden absolute bg-white border w-40 rounded mt-1 z-10"></ul>
          </div>
          <div>
            <%= f.submit "検索", class: "bg-blue-500 hover:bg-blue-700 text-white text-sm px-4 py-2 rounded" %>
          </div>
          <% if params[:q].present? %>
            <div>
              <%= link_to "リセット", players_path, class: "text-gray-500 text-sm hover:underline" %>
            </div>
          <% end %>
        <% end %>
      </div>
      
      <!-- 一覧 -->
      <div class="p-6 space-y-4">
        <% @players.each do |player| %>
          <div class="border border-gray-300 rounded-lg p-4 hover:shadow-sm transition">
            <div class="flex justify-between items-start">
              <div class="flex-1">
                <h3 class="font-medium text-gray-900 mb-1"><%= player.name %></h3>
                <p class="text-sm text-gray-600 mb-1"><%= player.team %> <%= player.position %></p>
                <p class="text-sm text-gray-500 mb-3"><%= player.created_at.strftime("%Y/%m/%d") %></p>
                <div class="flex space-x-2">
                  <%= link_to "編集", edit_player_path(player), class: "px-3 py-1 border border-gray-300 rounded text-sm text-gray-700 hover:bg-gray-50" %>
                </div>
              </div>
              <div class="text-right ml-4 text-sm">
                <div class="space-y-0.5">
                  <div><span class="font-medium">1B:</span> <%= sprintf("%.3f", player.stat_1b) %></div>
                  <div><span class="font-medium">2B:</span> <%= sprintf("%.3f", player.stat_2b) %></div>
                  <div><span class="font-medium">3B:</span> <%= sprintf("%.3f", player.stat_3b) %></div>
                  <div><span class="font-medium">HR:</span> <%= sprintf("%.3f", player.stat_hr) %></div>
                  <div><span class="font-medium">out:</span> <%= sprintf("%.3f", player.stat_out) %></div>
                </div>
              </div>
            </div>
          </div>
        <% end %>
        
        <!-- 空状態 -->
        <% if @players.empty? %>
          <div class="border border-gray-300 rounded-lg p-8 text-center">
            <p class="text-gray-500 mb-2">まだ選手が作成されていません</p>
            <%= link_to "選手を作成する", new_player_path, class: "bg-black text-white px-4 py-2 rounded hover:bg-gray-800 text-sm" %>
          </div>
        <% end %>

        <!-- ページネーション -->
        <div class="pt-4">
          <%= paginate @players %>
        </div>
      </div>
    </div> 

  </div>
</div>